---
pageable: true
per_page: 5
---

= partial '/partials/blog_navigation'

/ Page Header
%header.intro-header{:style => "background-image: url('/img/blog/blog-cover.jpg')"}
  .site-heading
    .text-center
      %h1<
        0x1
        %span.highlight> BAD
        F&#398;&#398;D
      %hr.small/
      %span.subheading A blog about security &amp; curiosity

/ Main Content
.container
  .row
    .col-lg-8.col-lg-offset-2.col-md-10.col-md-offset-1
      - if defined? page_articles
        - page_articles.each_with_index do |article, i|
          .post-preview
            = link_to article.url do
              %h2.post-title= article.title
            = article.summary
            %a.btn.btn-link.btn-block{href: article.url}
              Read more &raquo;
            %p.post-meta
              %span="Posted on #{article.date.strftime('%b %e')}"
          %hr/
        / Pager
        %ul.pager
          - if paginate && num_pages > 1
            - if prev_page
              %li.prev= link_to '&#8592; Newer Posts', prev_page
            - if next_page
              %li.next= link_to 'Older Posts &#8594;', next_page
%hr/
= partial '/partials/blog_footer'
